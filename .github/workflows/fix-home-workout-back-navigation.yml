name: Fix Home Workout back navigation

on:
  push:
    branches: ["main"]
    paths:
      - "scripts/fix_home_workout_back_navigation.py"
      - ".github/workflows/fix-home-workout-back-navigation.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply navigation fix
        run: python scripts/fix_home_workout_back_navigation.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html home-workout.html
          if git diff --cached --quiet; then
            echo "No changes required"
          else
            git commit -m "Use back arrows for Home Workout navigation"
            git push
          fi
